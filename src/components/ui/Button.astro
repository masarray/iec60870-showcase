---
const { href = "#", variant = "primary", pulse = false } = Astro.props;
---
<a
  href={href}
  class:list={[
    "btn",
    variant === "primary" ? "btn-primary" : "btn-secondary"
  ]}
  data-pulse={pulse ? "true" : "false"}
  data-shine={pulse ? "true" : "false"}
>
  <slot />
</a>
